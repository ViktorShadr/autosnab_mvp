"""Native Telegram bot module, replacing the retired cloud-n8n workflow.

Transport/session layer only: all invoice-processing logic stays in
`app.services.bot_gateway_service` and the pipeline it wraps.
"""
