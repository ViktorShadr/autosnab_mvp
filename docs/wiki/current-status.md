---
title: autosnab_mvp Current Status
source: session
created: 2026-07-02
tags: [status]
status: current
---

# autosnab_mvp Current Status

- Wiki bootstrap integrated into this repo on 2026-07-02.
- The knowledge base now lives under `docs/wiki/` in `autosnab_mvp`.
- Local raw-root created at `../autosnab_mvp_raw` for source documents outside Git.
- Next maintenance step: keep wiki writeback in sync with code changes and register any new raw files in `manifests/raw_sources.csv`.
- New SBIS EDO requirements were received on 2026-07-02 and compiled into `docs/wiki/sbis-edo-integration.md`.
- Additional screenshot intake on 2026-07-02 reinforced the centralized OCR/document-flow requirement and pointed to an existing Google Docs table named `АвтоСнаб Кафе Ромашка`.
- Architectural conclusion: SBIS EDO should be implemented as a source adapter over a shared document core, not wired directly into the current invoice-review flow.
- Parallel PDF export development means the shared document contract must be frozen early so the two tracks do not diverge.
- Final execution plan is now recorded in `docs/wiki/sbis-edo-integration.md`: freeze the contract, add SBIS as an adapter, keep the PDF flow unchanged, and validate both sources through one writer.
- New screenshot on 2026-07-03 updates delivery priority: MVP recognition/table placement this week, SBIS EDO next week.
- Meeting notes on 2026-07-03 add a nearer-term focus on multi-page document UX and parsing strategy before SBIS work starts.
- Latest intake on 2026-07-03 adds table-layout constraints: keep header names stable, use the existing Apps Script behavior, and treat the table as a document-level validation layer with a planned `Вернуть на проверку` action.
- Local wiki raw-root was re-initialized on this PC on 2026-07-03 at `../autosnab_mvp_raw`, including the `inbox/` drop zone for new raw attachments.
- Historical raw files already listed in `manifests/raw_sources.csv` are not mounted on this PC yet, so only new inbox intake is ready right now.
- Three new raw files were added to `../autosnab_mvp_raw/inbox/` on 2026-07-03 and registered in the manifest: `АвтоСнаб Кафе Ромашка .xlsx`, `Копия План АвтоСнаб .md`, and `Созвон с Лилией.md`.
- Those three raw files were compiled into wiki conclusions on 2026-07-03: broader supplier-catalog roadmap, a filled project overview, and more explicit validation-table behavior.
- The immediate working priority is now explicit: build an MVP that fills the validation tables from downloaded invoice documents.
- Deeper review of the Lilia walkthrough confirms that the highest-risk MVP areas are document-level status gating, stable table headers, duplicate control, manual reference completion, and recalculation logic.
- A concrete implementation checklist for that MVP is now recorded in `docs/wiki/invoice-table-mvp-checklist.md`.
- The Google Sheets writer now supports a shared-table mode: new invoice blocks can be inserted at the top of an existing sheet with one empty separator row between documents.
- A practical launch/test runbook is now recorded in `docs/wiki/runbook.md`.
- Comparison against the tested spreadsheet copy shows that shared-sheet prepend works structurally, but the inserted values are mapped against the wrong column order for the real `Накладная` sheet.
- The shared-sheet writer has now been rewritten to map rows into the real `Накладная` column order instead of the old `Накладные` register order; live Google retest is still required.
- The shared-sheet writer now computes the Google write range from the actual row width, fixing the `AL` vs `AM` mismatch that blocked the live retest.
- The OCR parser now filters out noisy item rows before they reach `Receiving` or the Google Sheet, reducing accidental garbage rows in the table.
- A local document-extraction service has been added: MinerU can now be enabled as the primary backend, with the current OCR/parser chain kept as fallback.
- The MinerU contract uses the active Python environment: `{python_executable} -m mineru.cli.client -p <input_path> -o <output_path> -b pipeline -l cyrillic`.
- The project installs CPU PyTorch plus `mineru[pipeline]`; the unused `mineru[all]` profile was removed because it pulled Linux CUDA/vLLM dependencies that this backend does not use.
- Today’s implementation track established the full local MVP loop: wiki writeback, raw intake, runbook, shared-sheet prepend mode, diagnosis of the column-contract bug, and a mapper rewrite aligned to the real `Накладная` sheet.

## Today summary

- The project is a document-processing pipeline, not a generic document store.
- The current MVP priority is to show reliable document recognition and placement into the existing validation table.
- Multi-page UX, parsing reliability, and stable table headers are the critical short-term risks.
- SBIS EDO is the next phase, but only after the shared document contract is frozen.
- PDF and SBIS must stay as source adapters over one common document core.
- New raw attachments can now be placed into `../autosnab_mvp_raw/inbox/` and registered from this machine.
- The latest inbox intake is registered and waiting for compile/writeback into wiki pages as needed.
- The repo now has a clearer split between the short-term document-validation MVP and the longer-term supplier-catalog/search roadmap.
- The nearest delivery target is not the broader catalog roadmap but the MVP flow from downloaded invoices into the validation table.
- The current write bug is a column-contract mismatch, not a failure of top insertion or separator-row insertion.
- The immediate blocking bug is now the fixed write-range width on the shared `Накладная` sheet, not the mapper shape itself.
- OCR quality is now constrained by a post-filter on item rows, so the remaining issue should be true recognition limits rather than unfiltered garbage rows.
- The repo now has a switchable local extraction layer, and MinerU is wired in as a documented CLI/output-directory backend that can be enabled from `.env`.
- The local extraction layer now targets MinerU's documented CLI/output-directory flow instead of stdout-only parsing.
- MinerU 3.4.2 is verified end to end on the local CPU environment with a real one-page UPD: provider `mineru`, invoice `1928`, date `2026-06-23`, supplier INN `3900040690`, and item `Еноки вес` (`3.14 кг x 650 = 2041`) were returned without OCR fallback.
- MinerU's first local run downloaded about 2.5 GB of models; cached runs complete in roughly 30 seconds on this machine. The backend timeout is 900 seconds to cover first-run model initialization.
- The MinerU output adapter now consumes Markdown plus `*_content_list.json`, avoiding accidental selection of service `*_model.json` and extracting UPD table rows from MinerU HTML.
- The remaining live task is to retest the Google write flow against the user-owned spreadsheet copy and verify that values now land in the correct business columns.
- The 2026-07-04 wrap-up is now captured in wiki: the local extraction path is MinerU-ready, the Google Sheets writer is documented and fixed at the column-contract level, and only safe docs were prepared for commit.
- MinerU runtime health is now checked against the actual HuggingFace model cache, not only package/import presence, so incomplete cache states report `mineru.ready = false`.
- Google OAuth now uses env-only credentials: client ID, client secret, access token, refresh token, and token expiry are read from `.env`; runtime no longer reads OAuth or service-account JSON files.
- `.env` and the legacy OAuth JSON files were removed from Git tracking. Because credentials existed in repository history, they must be revoked and reissued.
- The root `README.md` now matches the active product shape: invoice-validation MVP first, MinerU/OCR extraction backends, env-only OAuth, shared-sheet Google flow, and the remaining live retest/testing gaps.
- The upload UI now exposes per-document extraction choice: `Google OCR`, `MinerU`, or `hybrid` (`MinerU -> Google OCR fallback`) instead of relying only on the global `.env` backend switch.
- Docker startup is now aligned to the real backend shape: one `docker compose up --build` command starts FastAPI with mounted `.env` plus persistent volumes for SQLite, uploads, exports, and MinerU model cache.
- New root copies reviewed on 2026-07-04 (`MVP Бух калькулятор (2).md` and `АвтоСнаб Кафе Ромашка .xlsx`) make the current validation-table contract more explicit: document statuses belong only to the first row of each block, `Корректировка` is row-specific, and the live business contract is row 2 of `Накладная` across `A:AN`.
- Team direction has now shifted from regex/MinerU-first parsing to OpenAI-first parsing: OCR and MinerU should remain evidence sources or fallbacks, while the final field extraction should be delegated to an OpenAI model and then normalized deterministically before Google Sheets writing.
- OpenAI structured parsing is now integrated as the default extraction mode: PDF text, MinerU, and Google OCR provide evidence; Pydantic validates the strict result; deterministic code normalizes values and assigns review statuses.
- The shared `Накладная` writer now reads and validates row-2 headers before mapping `A:AN`; document-level fields are first-row-only and `Корректировка` stays item-specific.
- Duplicate outcomes, OCR errors, amount mismatches, debug traces, and the `Загрузить` send gate are covered by focused tests. A live OpenAI + Google Sheet retest remains required with valid external credentials.
- Docker build reliability was improved on 2026-07-04 by simplifying the backend ASGI dependency from `uvicorn[standard]==0.34.0` to plain `uvicorn==0.34.3`; this avoids optional extra-resolution during image build while preserving the runtime server command.
- OpenAI item output now includes cleaned and normalized name candidates, descriptors, package structure, source/document quantities, conversion candidates, codes, confidence, and row-specific review reasons.
- A deterministic `item_normalization_service.py` now rechecks package extraction and conversion, reads `Товары` plus `Справочник фасовок` from the configured Google spreadsheet, and fills the US name/unit/quantity fields only through backend mapping.
- Missing products map to row correction `Нет в справочнике`; ambiguous products, missing packages, and incompatible units map to `Сопоставление`. The Google writer still controls fixed columns and first-row-only document statuses.
- The expanded item pipeline passes all 58 backend tests outside the pre-existing hanging `test_receiving.py`; a live Google retest remains blocked by the environment's Google TLS handshake timeout.
- The upload page now shows an inline preview for selected image/PDF invoices and includes a built-in Google OAuth block with live status polling plus popup-based authorization, so operators can stay on one screen while preparing a document upload.
- The upload page now also renders backend pipeline logs for document processing stages. If evidence is empty before OpenAI or the model returns an empty structured payload, the upload flow stops before saving the document and returns a visible error with retry guidance instead of silently creating an empty review.
- Upload tracing is now live instead of post-factum only: the page creates a per-upload `trace_id`, polls a backend trace endpoint during processing, and shows incremental stages for evidence collection, OpenAI parsing, reference mapping, and Google Sheets write attempts.
- Review of `Копия АвтоСнаб Кафе Ромашка  (2).xlsx` showed that some rows in `Накладная` were generated from older stored review payloads without `us_product_name/product_found`, even though the current matcher can map examples like `Еноки вес` -> `Еноки вес.` and `КЕФИР ФЕРМЕРСКИЙ` -> `Кефир`. Review-sheet generation now needs backend backfill so old documents are re-mapped against current `Товары` and `Справочник фасовок`.
- Screenshot `img.png` confirmed two additional deterministic requirements for shared-sheet output: `Наименование товара в УС` must never stay empty when a normalized candidate name exists, even if the product is not found in the catalog; and `ИНН Поставщика` must be re-normalized during sheet build/backfill so old stored `ИНН/КПП` merges do not leak into the visible column.
- Upload page live tracing is now backed by a background job: the page starts a `upload-photo-live` request, gets a `trace_id` immediately, and the browser can watch the trace update while the backend continues parsing, mapping, and writing the document.
- Today's work ended with the shared-sheet pipeline behaving as a live, operator-visible job: image/PDF preview stays on the upload page, evidence and OpenAI parsing are logged progressively, `Наименование товара в УС` falls back to a normalized candidate when there is no catalog match, and supplier INN is re-cleaned from merged OCR values before the review sheet is rebuilt.
- Five invoice photos reviewed on 2026-07-04 represent four documents: two single-page UPDs, one two-page товарная накладная, and one retail receipt. They are suitable as a small real-photo golden set but expose missing multi-page grouping and receipt-specific behavior.
- ~~The current `openai` mode does not send source images to OpenAI~~ **Corrected 2026-07-17**: this is stale. `_build_openai_input` in `openai_invoice_parser_service.py` sends real page images (`input_image` base64 blocks) for every image-type page alongside OCR text, up to `openai_max_image_pages=12`. Unclear when this changed since 2026-07-04; not tracked as a completed Phase 4 milestone anywhere until now.
- A live isolated run on the new photos stopped before OpenAI: MinerU in the Docker image fails because `libxcb.so.1` is absent, and Google Drive OCR then fails with `_ssl.c:993: The handshake operation timed out`. Current runtime quality cannot be accepted until this evidence path is repaired.
- The upload endpoint accepts one file only. Pages 1 and 2 of invoice `УПМК003248` would currently become separate documents, with page 2 lacking document-level requisites; multi-file grouping or PDF assembly is required before production use.
- The ordered remediation plan is fixed in `docs/wiki/invoice-recognition-hardening-plan.md`: restore and instrument evidence providers first, add logical multi-page upload and deterministic image preparation second, then send text plus source pages to the strict OpenAI parser, harden validation, run the four-document golden set, and only then perform a live Google Sheets retest.
- `Расчет коэфф.md` adds a mandatory deterministic conversion stage to that plan: backend must calculate the coefficient, `Кол-во в УС`, and `Цена в УС`, preserve the line amount, and use a separate product-exception reference for piece-to-weight conversions. Current code calculates accounting quantity partially but always leaves `Цена в УС` empty.
- Phase-0..6 hardening was advanced again on 2026-07-04: upload traces now expose a versioned trace contract plus `logical_document_id` / `evidence_version` metadata alongside live logs.
- The upload UI now behaves as a logical multi-page document surface instead of a plain file picker: operators can keep multiple pages in one upload, reorder them before submit, and remove wrong pages without leaving the screen.
- Image preparation now includes deskew plus extra quality signals (`text_coverage_ratio`, `clipping_ratio`) and turns poor-photo findings into explicit pipeline/page warnings instead of silent degradation.
- Multi-page evidence merging now detects missing continuation-page markers from OCR text and surfaces those gaps as consistency warnings before Google Sheets write is attempted.
- Golden-set support now goes beyond a static JSON list: expected `Накладная` rows were added to the real-photo fixtures, replay evaluation is implemented, a compact per-provider report can be generated, and a no-write live evaluator contract now forces `create_google_sheet=False`.
- Two maintenance scripts were added for the hardening plan: `scripts/docker_runtime_smoke.py` for Docker/provider smoke checks and `scripts/run_invoice_golden_eval.py` for replay-based golden reporting.
- OpenAI evidence collection and `hybrid` extraction now skip unhealthy MinerU immediately instead of waiting through a doomed MinerU run before OCR fallback.
- The remaining MinerU runtime issue in Docker is model/cache repair, not hidden hanging behavior: unhealthy MinerU is now surfaced explicitly in provider health and pipeline logs.
- Analysis of `Копия АвтоСнаб Кафе Ромашка 2.xlsx` on 2026-07-04 shows two distinct data issues: the kefir receipt rows already contain `normalized_name_candidate = Кефир Фермерский` in stored parser metadata but still have blank `us_product_name/product_found`, so the export reflects stale unmapped payload rather than a true catalog miss; and duplicate flags come from historical `ReceivingDocument` rows in SQLite, not from the workbook itself.
- Review-sheet build now has a local fallback for `Наименование товара в УС`: if an old/stale payload has no `us_product_name` yet, the sheet uses `normalized_name_candidate` / `clean_name` / raw parser name instead of leaving the column blank. Deterministic catalog mapping still overrides that fallback when Google references are available.
- SQLite runtime safety is now stricter: backend startup performs a real write probe, `/health/runtime` reports database readiness, Docker healthcheck targets that runtime endpoint, and background upload traces translate `attempt to write a readonly database` into an explicit volume/permission diagnosis for `/data/autosnab_mvp.db`.
- New analyst feedback on 2026-07-05 tightened the shared-sheet write plan again: row-level `Нет в справочнике` is confirmed, `Основание` must stop echoing document form, `Цена в УС` needs deterministic completion when conversion is known, receipt documents need a separate VAT-aware branch, and the current `Проверить` gate must be compared against the live Apps Script conditions.
- Analysis of `Копия АвтоСнаб Кафе Ромашка 3.xlsx` now grounds those remarks in the actual latest export: supplier documents still write buyer-side values into `Грузоотправитель/Получатель`, one `ТОРГ-12` block shows unstable conversion (`3.954 -> 15.634116` in `Кол-во в УС`), receipt rows still leave `Цена в УС` plus VAT columns empty, and unmatched rows do not yet map consistently to `Нет в справочнике`.
- A code-level implementation backlog is now fixed in `docs/wiki/google-sheet-fill-tech-backlog.md`: P0 is semantic header mapping, unmatched-product correction, and conversion stability; P1 is receipt-specific normalization plus duplicate-key normalization; P2 is fallback-name cleanup and diagnosis of the live `Проверить` gate.
- The first implementation pass of that backlog is now in code and test-covered: shared-sheet mapping no longer copies `Грузополучатель` into `Грузоотправитель`, deterministic backfill upgrades `product_found = Нет` into `Нет в справочнике`, receipt normalization now fills fallback VAT defaults when evidence is absent, basis text that merely repeats document form is cleared, and duplicate detection now canonicalizes invoice numbers such as `UPMK...` vs `УПМК...` and `ЧЕК 0245` vs `0245`.
- A new architecture hypothesis is now fixed in wiki: the next provider evolution should separate `OCR / layout` from `parser backend`, keep one normalized evidence contract, and evaluate `Yandex Vision OCR`, `YandexGPT`, and `GigaChat` as pluggable providers rather than doing a monolithic vendor swap.
- The original workbook `АвтоСнаб Кафе Ромашка  (ориг).xlsx` was added to local raw intake on 2026-07-06 and registered as `src_bd91ee3517`.
- That original workbook is now the canonical offline source for the `Накладная` contract; historical copies such as `Копия АвтоСнаб Кафе Ромашка 3.xlsx` remain diagnostic evidence for bad exports, not the primary contract baseline.
- The contract must now be read from three layers together: row 1 business annotations, row 2 machine headers, and the immutable Apps Script workflow rules.
- Apps Script is accepted as the baseline for sheet workflow/gating behavior, but not as the parser for invoice contents; document extraction still belongs to OCR/MinerU/OpenAI plus deterministic normalization.
- The original workbook's `Накладная` mapping is now extracted into `docs/wiki/original-workbook-contract.md`, including row-1 rules, row-2 machine headers, calculation columns, and reference-driven fields.
- A direct code comparison against that contract is now captured in `docs/wiki/backend-vs-original-workbook-gap-analysis.md`: the shared-sheet write path now builds canonical `Накладная` rows directly, while the remaining gaps are contract-centralization and field-ownership issues.
- The shared Google Sheets writer no longer depends on the legacy `Накладные -> remap -> Накладная` path as its primary output route; canonical shared rows are now built directly from normalized review data.
- Today's implementation closed the main structural rewrite for sheet output: `build_shared_invoice_rows(...)` now emits canonical `Накладная` rows directly from normalized review/header/item data, while the legacy register builder remains only as a compatibility path for the old review-sheet shape.
- Targeted verification on 2026-07-06 passed for the direct shared-sheet path: `backend/tests/test_google_sheets_service.py` plus two `test_receiving.py` sheet-behavior regressions covering supplier INN safety and TORG-12 continuation-page cleanup.
- The next writeback objective is therefore sharper: validate backend block-building against the original workbook structure and the immutable Apps Script behavior, then use the copied workbooks only to prove regressions are removed.
- Branch comparison on 2026-07-07 clarified the multi-page integration strategy: `main` still contains an older `multipage_invoice` upload path plus TORG-12 continuation-page OCR heuristics, while `codex/invoice-recognition-hardening` already has the newer logical multi-page upload surface, page-evidence merge, trace metadata, and OpenAI single-parse flow. Any branch integration should therefore be selective and code-level, not a blind full merge from `main`.
- A repo-level integration plan is now fixed in `docs/wiki/github-and-raw-strategy.md`: push the hardening branch through the author's fork first, open a PR into `AndreyGomzikov/autosnab_mvp:main`, keep local artifacts out of Git, and resolve conflicts manually with the hardening branch as the default winner for the newer extraction architecture.
- `over_version` is now being aligned onto the `codex/invoice-recognition-hardening` code line so the colleague-facing branch keeps the newer extraction, multi-page, tracing, and canonical shared-sheet logic instead of the older `main` implementation.
- The shared invoice/register first-row helper now tolerates missing newer header keys in older payloads by returning an empty value instead of raising `KeyError`, which keeps merged-branch tests and historical review rebuilds compatible.
- A newly added root note `MVP Бух калькулятор (2).md` re-confirms the operator workflow contract: document-level statuses live only on the first row, `Корректировка` is row-local, `Проверить выбранные документы` upgrades clean documents to `Загрузить`, and `Вернуть на проверку` resets the document to a manual review cycle.
- A newly added root screenshot `img_3.png` captures the current business checklist above the parser itself: connect the bot/MVP logic around the approved TOR, finish iiko integration from the sheet via script, complete document recognition, pull SBIS documents into the table, and run an end-to-end demo for Galina before client rollout.
- `MVP Бух калькулятор (2).md` is now explicitly obsolete and must not be treated as the active business source of truth; the current BA baseline is `MVP Бух калькулятор.md`.
- Code review on 2026-07-08 confirms that the shared invoice-review backend is already functional as the central pipeline: multi-page upload UI, extraction/evidence collection, OpenAI normalization, Google Sheets review writing, iiko reference enrichment, and iiko incoming-invoice XML preview/export all exist in code.
- A code-path check on 2026-07-08 makes the current multi-file boundary explicit: `/invoice-review/upload-document-live` accepts multiple files as one logical document, merges page evidence, and can write the resulting review block into Google Sheets, but that multi-page path currently supports only the OpenAI parser flow. It is not a batch import for multiple independent invoices in one request.
- The same review also confirms that there is still no dedicated Telegram bot implementation and no SBIS/Saby adapter, client, scheduler, or sync-history layer in the repo yet; bot and SBIS work should therefore integrate with the existing backend contract instead of re-implementing document logic.
- A dedicated implementation plan for the user's scope now lives in `docs/wiki/bot-sbis-implementation-plan.md`: use the existing invoice-review backend as the single processing core, build the bot first as a thin upload/status adapter, then add SBIS as a read-only source adapter with raw storage, dedupe, and sync history.
- The latest planning task is now fixed more concretely in `docs/wiki/n8n-bot-implementation-plan.md`: implement the first bot through `n8n` as a Telegram session/orchestration layer over the existing invoice-review backend, with explicit upload/status/result contracts and no duplicated OCR/parsing/export logic inside the bot.
- The current near-term priority inside the bot track is to freeze the backend contract for logical-document upload plus `trace_id` polling, then assemble the `n8n` workflows in this order: Telegram router, document-session collector, finalize/upload, backend status polling, and result notification.
- `ТЗ бота.pdf` was reviewed on 2026-07-08 and confirms the bot boundary already assumed in wiki: it is an intake/status adapter only, while OCR/parsing/review/export remain in the existing backend and review module.
- That PDF also fixes additional bot requirements now captured in wiki: supported intake types must eventually include image/PDF/XML/Excel/receipt-QR, operators may need organization/point selection, and a durable upload journal must keep upload ID, user, file, source path, status, and error text.
- The first backend contract for the bot is now implemented and fixed in `docs/wiki/bot-backend-api-contract.md`: dedicated bot-facing upload/status endpoints exist over the current invoice-review pipeline, and a persistent `ingestion_uploads` journal now stores upload provenance plus terminal bot-facing statuses.
- A repo-native `n8n` bot artifact still exists as the next assembly layer, but it has now been consolidated to one current import file: `n8n/telegram-bot-mvp.workflow.json`.
- The node-by-node setup reference remains in `n8n/telegram-bot-node-setup.md`, but the runtime contract changed: the workflow no longer reads `$env`, and deployment-specific values now live in the opening `Workflow Config` node.
- The current Telegram UX is intentionally simpler than the earlier full-workflow experiments: the first uploaded file can auto-open a document draft, each next page is appended to the same logical document, and the operator completes the document with button `Продолжить` instead of free-form typed confirmation.
- Durable cross-message assembly remains based on the `telegram_bot_sessions` Data Table rather than in-memory workflow state, because this `n8n` deployment previously lost volatile session data between Telegram updates.
- `upstream/over_version` was merged into the active bot branch on 2026-07-09 with manual conflict resolution in `backend/app/routers/invoice_review.py`: the branch now keeps both the bot ingestion/journal flow and the colleague's newer invoice-upload UI/processing fixes.
- That merge also pulls in the clarified upload boundary now present in code: the UI supports one single-page invoice by default and switches to explicit multi-file mode only when `Многостраничная накладная` is enabled, while server-side validation rejects multiple files unless they are declared as one logical multi-page invoice.
- The merge intentionally did not take the tracked `autosnab_mvp.db` change from `upstream/over_version`; local SQLite state remains branch-local and is not treated as safe code to inherit from a colleague branch.
- Targeted post-merge verification passed on 2026-07-09: `backend/tests/test_google_sheets_service.py` reports 7 passing tests, and Python syntax compilation succeeds for the merged invoice-review and Google Sheets service modules.
- The Telegram bot workflow was then rebuilt again on 2026-07-09 around the merged upload logic: the repo now keeps one MVP import file `n8n/telegram-bot-mvp.workflow.json` instead of the earlier parallel bot JSON variants.
- That new workflow no longer relies on `$env`; all deployment-specific constants now live in the opening `Workflow Config` node so the target `n8n` instance can import the JSON without env-backed expressions.
- The MVP bot UX is now intentionally thinner and clearer: after each uploaded page it asks whether more pages will follow, the operator finishes the logical document with button `Продолжить`, and `n8n` does not choose any parser mode before calling backend.
- The bot backend status contract is now richer for Telegram delivery too: `GET /api/v1/invoice-review/bot/uploads/{upload_id}` includes `document_summary` plus `google_spreadsheet_url` / `google_spreadsheet_error`, so the bot can send a short parsed summary and the final table link without duplicating backend logic.
- The local `n8n` runtime is now up on this workstation at `http://localhost:5678` through Docker container `autosnab_n8n_local`, using the persisted home at `~/.n8n`.
- The required durable bot session table is no longer just planned: Data Table `telegram_bot_sessions` already exists in the local `n8n` database with the expected session columns and backing storage table.
- The rebuilt MVP workflow is also already present in that local `n8n` instance as draft workflow `autosnab telegram bot mvp`, so the next work can continue directly in the editor instead of re-importing JSON first.
- A reusable CSV header template for the cloud-`n8n` session store now lives at `n8n/telegram_bot_sessions.csv`, so the `telegram_bot_sessions` Data Table can be created by import instead of manual column entry.
- Local backend reachability from `n8n` is now confirmed too: `host.docker.internal:8000` resolves from the `n8n` container and returns healthy runtime status for the bot upload/status calls.
- The repo runtime now also supports the cloud-`n8n` path explicitly: `docker-compose.yml` has an optional `ngrok` profile that publishes the local backend, `PUBLIC_API_BASE_URL` is no longer hardcoded to `localhost` inside Docker, and `scripts/get_ngrok_public_url.py` prints the current public tunnel URL for workflow configuration.
- The importable bot workflow and setup docs now treat `Workflow Config -> backendBaseUrl` as a public HTTPS value for cloud `n8n`, with `host.docker.internal:8000` remaining only a local-container topology detail.
- The compose runtime is also more tolerant of this workstation's env-file layout now: `BACKEND_ENV_FILE` can override the default `.env` path when Docker cannot use the repo-root `.env` directly.
- A second import artifact now exists for cloud debugging: `n8n/telegram-bot-mvp.workflow.fixed.json` preserves the user's current hardcoded Telegram credential/token/backend URL while keeping the corrected `telegram_bot_sessions` wiring (`chatId` filters, `chat_id` matching upserts, row-scoped updates, reset-by-chat deletion).
- A third import artifact was saved on 2026-07-09 for direct handoff from the broken editor state: `n8n/telegram-bot-mvp.workflow.hardcoded-fixed.json` is a separate copy of the validated fixed workflow, preserving the current hardcoded Telegram token, backend URL, and Telegram credential binding while restoring the same corrected Data Table wiring and file-download settings.
- A fourth import artifact now exists for older/self-hosted `n8n` compatibility: `n8n/telegram-bot-mvp.workflow.hardcoded-fixed.n8n-2.3.6.json` keeps the same hardcoded Telegram/backend values but rewrites `Data Table` nodes to the `n8n 2.3.6` schema (`dataTableId`, explicit upsert conditions, row-safe reset) and reconnects upload polling from the normalized backend response instead of the Telegram reply node.
- Architecture review on 2026-07-09 shows why the bot/backend coupling now feels heavy: one logical upload is split across `ingestion_uploads`, the in-memory trace store, and `n8n` Data Table session state.
- That same review also confirms the main structural debt: `n8n` currently stores full page binaries before finalize, while backend status enrichment still depends on RAM-only traces and daemon-thread completion, so the contract is thinner in intent than in runtime reality.
- The recommended simplification path is now recorded in `docs/wiki/backend-bot-integration-review.md`: move draft file assembly and durable progress fully into backend, keep `n8n` as transport/session routing only, and extract shared upload orchestration out of `invoice_review.py`.
- All previous `n8n` workflow artifacts (`n8n/` directory: JSON workflows, node-setup doc, session CSV template, README) were deleted on 2026-07-09 at the user's request; the bot is being rebuilt from scratch.
- Today's priority is a Telegram bot that replaces the web upload page for single- and multi-page накладная scans, with all business logic staying in the backend. The concrete plan is now recorded in `docs/wiki/telegram-bot-cloud-n8n-plan.md`: backend owns all draft/session state via a new `collecting` status on the existing `ingestion_uploads` table, four new `/bot/drafts/*` + `/bot/uploads/latest` endpoints, and `n8n` becomes a stateless router with no Data Table. `docs/wiki/n8n-bot-implementation-plan.md` is marked superseded for its session-storage design but stays valid for UX/format reference.
- The plan also fixes required operations for today: local Docker backend plus `ngrok` public-tunnel profile already exist in `docker-compose.yml`; the new work is a shared-secret header check on `/bot/*` endpoints before exposing them publicly, since no auth exists there yet.
- The backend half of that plan is now implemented in code: `ingestion_uploads` gained a `collecting` draft status (no schema migration needed), and five endpoints exist under `/api/v1/invoice-review/bot/` — `POST drafts/pages`, `GET drafts/status`, `POST drafts/reset`, `POST drafts/finalize`, `GET uploads/latest` — plus the two pre-existing bulk-upload endpoints. `n8n` can now stay fully stateless: draft/session state lives only in backend, keyed by `chat_id`.
- A shared-secret header check (`X-Bot-Api-Key` vs. new setting `bot_api_shared_secret`) now guards all `/bot/*` endpoints; empty secret (default) keeps local dev unauthenticated, matching `.env.example`'s new `BOT_API_SHARED_SECRET` entry.
- `docs/wiki/bot-backend-api-contract.md` is updated with the new draft endpoints and the auth gap is marked closed.
- 8 new backend tests cover the draft flow (accumulate across requests, per-chat isolation, reset, empty-finalize rejection, finalize-and-visible-via-latest, unsupported format, latest-with-no-history) in `backend/tests/test_receiving.py`; all pass. The pre-existing failures in that file (10 tests, unrelated to this work — confirmed identical on the pre-change commit) are untouched.
- The `n8n/` directory now exists again with a fresh, single importable workflow: `n8n/telegram-bot-mvp.workflow.json` (29 nodes) plus `n8n/telegram-bot-node-setup.md` (import/credentials/checklist) and a short `n8n/README.md`. It is a stateless Telegram router built directly against the new `/bot/drafts/*` + `/bot/uploads/latest` contract: no Data Table, no session storage in `n8n` at all.
- This workflow JSON was hand-authored against the backend contract and validated for internal consistency (valid JSON, all 29 node names referenced in `connections` exist, no dangling edges) but has **not** been test-imported into a live n8n instance from this environment — there is no live n8n access here. `telegram-bot-node-setup.md` calls out the specific parameters to verify after import (multipart file field on `Send Page To Backend`, `onError: continueErrorOutput` on the two branching HTTP nodes, credential attachment).
- The bot is now confirmed working end to end on the user's machine: local Docker + `ngrok` tunnel is up, the workflow was imported into cloud `n8n`, and a real two-page invoice photo set was uploaded through Telegram, producing one logical document (`УТ-35634`, `ООО "Птицеводческий комплекс "Продукты питания""`, sum `4117.06`) written to the shared Google Sheet, confirmed via backend logs and the `/bot/uploads/{id}` trace.
- Following user feedback on UX, the workflow was extended (now 33 nodes) to remove all manual typing and add visible processing-stage updates: a persistent Telegram reply keyboard (`Готово` / `Статус` / `Сбросить`) is now attached to every bot reply via the **Send Reply** node, and a new stage-tracking sub-loop (`Compute Stage` → `If Stage Changed` → `Set Stage Reply Text` → `Send Stage Update`) sends one extra progress message per pipeline stage change (`принят в обработку` → `выгружаем данные` → `обрабатываем через ИИ` → `загружаем в таблицу`) derived from the backend's existing `pipeline_logs`, so the operator never has to wonder if the bot is stuck. This still needs no new backend state — it reads the same `/bot/uploads/{id}` response already used for polling.
- The hand-authored reply-keyboard JSON on **Send Reply** turned out to be wrong: importing it threw `Could not find property option`, confirming the earlier flagged risk (the Telegram node's `replyMarkup`/`replyKeyboard` fields are not valid as top-level node parameters). Removed that JSON entirely from `n8n/telegram-bot-mvp.workflow.json`; the workflow now imports clean at 33 nodes. `n8n/telegram-bot-node-setup.md` now gives exact manual UI steps (Additional Fields → Reply Markup → Reply Keyboard → two rows) since the editor UI cannot produce invalid JSON, unlike hand-authoring it blind.
- Remaining for today: re-import the fixed workflow, add the reply keyboard manually per the setup guide, reconfirm credentials on the two new nodes (**Send Stage Update** needs the Telegram credential too), and re-run the smoke tests, watching for the new stage-progress messages during a real upload.
- Live testing surfaced a UX confusion, captured in root screenshot `img_4.png` (registered in `manifests/raw_sources.csv` as `src_20260709_img4`): after `Сбросить` with no new pages sent, `Статус` fell back to the last *finished* upload for that chat and read as if it were answering about the just-cleared draft. Behavior was correct (no active draft → show last known result) but the wording wasn't. Fixed by tagging that specific branch (`Mark As Status Command`, workflow now 34 nodes) and prefixing the message with `Активного черновика нет. Последний обработанный документ:` so it reads as history, not a live answer.
- The reply-keyboard question is now resolved with certainty: the user pasted back their own live cloud-n8n export (with real credential IDs, real `backendBaseUrl`, and a working reply keyboard already configured through the editor UI). `n8n/telegram-bot-mvp.workflow.json` was rebuilt from that exact export plus the `Mark As Status Command` fix layered on top, instead of continuing to hand-author independently. The confirmed-correct keyboard schema is `replyMarkup: "replyKeyboard"` with each row shaped `{"row": {"buttons": [{"text": "..."}]}}` (the earlier `values`-keyed guess was the actual bug, not the top-level placement). `n8n/telegram-bot-node-setup.md` and memory were updated to stop instructing manual keyboard setup, since it now ships correctly in the JSON.
- Established working process for the rest of this bot's iteration: the user tests live against their own n8n instance and pastes back their current export (or a screenshot) when something looks wrong; fixes get layered onto that exact file rather than regenerated from the wiki plan or assistant memory alone.
- Root `README.md` now has a full, self-contained "Telegram-бот через облачный n8n" section (8 numbered steps: `.env` prep, backend+ngrok up, get public URL, Google OAuth, import workflow, create the two n8n credentials, set `Workflow Config`, activate+smoke-test) plus a troubleshooting list, written so a colleague can set the bot up from scratch without needing this conversation's context.
- This workstation's `.env` was missing `NGROK_AUTHTOKEN` and `BOT_API_SHARED_SECRET` entirely (only `.env.example` documented them); both were filled in on 2026-07-09 so the `public-tunnel` Docker profile can actually start.
- A real infrastructure bug was found and fixed on 2026-07-09: multipart file uploads through the `ngrok` tunnel failed with `ERR_NGROK_3004` (`invalid or incomplete HTTP response`) while small GET/JSON requests worked fine. Root cause was a Docker network MTU mismatch — the default bridge network used MTU 1500, but this machine's active VPN interface (`amn0`) caps the real path at MTU 1376, so larger multipart bodies were silently dropped. Fixed in `docker-compose.yml` by adding a `networks.default` block with `driver_opts: com.docker.network.driver.mtu: 1376`. Reproduced and confirmed independently with raw `curl` before and after the fix, outside of n8n.
- A separate, unrelated 401 (`Неверный или отсутствующий X-Bot-Api-Key`) on **Send Page To Backend** was traced to the n8n-side `Backend URL` HTTP Header Auth credential not holding the current `BOT_API_SHARED_SECRET` value — not a backend or MTU issue. Fixed by re-entering and explicitly saving the header value in that n8n credential. The bot is now confirmed working end to end again after both fixes (backend health `200`, file upload through the tunnel returns `200` with `pages_count` incrementing).
- Screenshot `img_7.png` surfaced two more UX defects, both fixed in `n8n/telegram-bot-mvp.workflow.json` (now 35 nodes): every Telegram send node now sets `additionalFields.appendAttribution: false` to remove the "This message was sent automatically with n8n" footer; and the `Принял, обрабатываю документ...` reply — previously fanned out in parallel to `Send Reply` and `Prepare Poll` — now goes through a dedicated `Send Started Reply` node wired strictly before `Prepare Poll`, fixing a real ordering bug where the poll-loop branch (containing a `Wait` node) could finish and deliver the final result before the "started" message actually reached Telegram.
- The fix initially appeared not to work (`img_8.png`), which led to a false lead questioning whether `appendAttribution` was even a valid/licensed n8n parameter. The actual root cause, found via the n8n Executions tab (`img_9.png`/`img_10.png`): this cloud-n8n instance has a Draft/Publish split, and the fixed graph existed in the editor but the live Telegram-triggered execution was still running the old, unpublished version (no `Send Started Reply` node in the executed graph, attribution still present in the output). Clicking **Publish** made both fixes take effect — confirmed working by the user. No workflow-file change was needed after this; the lesson is procedural (publish before testing), recorded in `n8n/telegram-bot-node-setup.md`.
- A fresh `ТЗ бота.pdf` vs. code re-check on 2026-07-09 confirms the core contract (thin bot adapter, journal fields, PDF text-vs-OCR branching, status model) is implemented correctly, but four ТЗ items remain open: XML/Excel parsing (recognized but explicitly rejected with `unsupported_format`), the receipt-QR scenario (not implemented at all — no QR code anywhere in bot code, a receipt photo just goes through generic OCR), user-facing organization/point selection (fields exist but nothing ever prompts for or fills them), and per-user upload authorization (only a single shared `X-Bot-Api-Key` secret exists, which authenticates n8n↔backend, not individual Telegram users).
- Root-caused the user's report of "same invoice uploaded repeatedly, slightly different results each time": production debug logs showed `file_110.jpg` uploaded three times in a row got OCR text of length 1 (empty), 1 (empty), then 2351 (real) characters. Cause: Google Drive's OCR-on-upload conversion is async, and `recognize_invoice_with_google_drive_ocr` exported the text immediately after upload with no wait, sometimes racing ahead of the conversion and getting back just a UTF-8 BOM — which was then misclassified as a successful, real OCR result because `"﻿".strip()` is truthy in Python. Also confirmed MinerU is not a real fallback right now: its model cache is missing `unet.onnx` entirely, so Google Drive OCR is currently the pipeline's only evidence provider, with no redundancy.
- Fixed in `backend/app/services/ocr_service.py` (`_export_ocr_text_with_retry`, `_has_meaningful_ocr_text`) plus new settings in `config.py`/`.env.example`: export is now retried (default 6 attempts, 4s apart) until real text (≥20 chars after stripping the BOM) comes back, and the BOM is always stripped from what reaches the rest of the pipeline. Regression tests added in `backend/tests/test_ocr_provider.py` replay the exact empty/empty/real sequence from production. Deployed: `autosnab_backend_mvp4` was rebuilt and restarted on 2026-07-09 with the fix.
- Live re-test against the actual `file_110.jpg` inside the rebuilt container showed the retry helps but is not a full guarantee: across a 3-run timing probe (12 attempts/5s budget), 2 runs got real text (7s and 32s) and 1 run still came back empty after ~104s of retrying the same Drive document. This means Google Drive's OCR-on-upload conversion can genuinely fail for a given upload, not just lag — a real limitation of this provider, not only a race. Added a second, smaller fix for this: when OCR still comes back empty and the pipeline falls back to vision-only parsing (image bytes sent to OpenAI with no OCR text), `document_extraction_service.py` now appends an explicit `consistency_warning` so the document surfaces as `needs_review` instead of silently looking like a normal successful run.
- MinerU is now a real working fallback provider, not just formally "ready". `python3 -m mineru.cli.models_download -s huggingface -m pipeline` was run inside `autosnab_backend_mvp4` against the persistent `autosnab_hf_cache` volume. Hit one complication worth remembering: a container rebuild mid-download interrupted the first attempt, and the downloader's own resume logic only checks "does the model directory exist", not file integrity — it silently treated a half-downloaded `models/MFR/unimernet_hf_small_2503` (config/tokenizer files present, actual weights missing) as complete and skipped it. This was invisible to `mineru_health()` too, since that check only verifies one file (`unet.onnx`) as a proxy for the whole 6-model cache being present, so it reported `ready: true` while a real document still failed with `OSError: no file named ... model.safetensors ...`. Fixed by deleting that one corrupted model directory and re-running the downloader, which then re-fetched it properly. Confirmed working end to end: `_extract_with_mineru` on the same `file_110.jpg` that broke Google Drive OCR now returns 3466 characters of structured table text in ~41s, and `mineru_health()` reports `ready: true` for real.
- Residual risk worth flagging for later: `mineru_health()`'s single-file check is still shallow relative to the real multi-model dependency surface — a future partial cache corruption (e.g. another interrupted download) could again report `ready: true` while actually failing at inference time. Not fixed now; out of scope for this session but a natural next hardening step if MinerU flakiness recurs.
- Provider order for image/scan evidence in `_collect_openai_evidence` was briefly "MinerU first" after MinerU was repaired, per an explicit user choice to observe real bot uploads before deciding. That observation is done: provider order is now (1) direct PDF text if the file is a text-layer PDF, (2) Google Drive OCR, (3) MinerU only as a fallback when OCR returns empty/erroring. Google Drive OCR is faster (network call vs ~40s CPU-only MinerU inference) and, with the export-retry fix, reliable enough to be the primary path again; MinerU now exists purely as a safety net for the cases OCR still can't recover, which the `consistency_warning` added for empty-OCR runs continues to surface as `needs_review` regardless of which provider ultimately supplied the text.
- Re-examined `Копия АвтоСнаб Кафе Ромашка 3.xlsx` (same filename as the 2026-07-05 analysis, but this copy's real document rows turned out to be from *today's* live test uploads, including the same `file_110.jpg` repeats used to diagnose the OCR race). Found and confirmed the file also has ~1200 rows of `n8n/telegram-bot-mvp.workflow.json` text accidentally pasted line-by-line into column `Получатель` — local-file-only per the user, not in the live Google Sheet, so no spreadsheet cleanup needed. Of the four bugs from the old `workbook-export-3-analysis.md` page, two were already fixed in current code (`Основание` echo, `Нет в справочнике` mapping) and one (TORG-12 quantity squaring) did not reproduce in fresh data. Two real, still-live bugs were found instead by comparing the three fresh `file_110.jpg` runs directly: shipper/receiver field confusion (the OpenAI prompt had no grounding for `Грузоотправитель`/`Получатель`/`Основание` against real form labels — fixed with an explicit prompt addition) and `document_form` wording instability (`"ТОРГ-12"` vs `"ТОВАРНАЯ НАКЛАДНАЯ"` for the identical document across runs — fixed with a first-pass `_normalize_document_form(...)` canonicalizer).
- That first-pass `document_form` fix turned out to target the wrong canonical values. Re-checking against `АвтоСнаб Кафе Ромашка  (ориг).xlsx`'s own `data_validations` (not just row-1/row-2 text) surfaced the real dropdown Google Sheets enforces for `Форма документа`: `"Торг-12,УПД,Кассовый чек,Акт закупа,Акт приема-передачи,Транспортная накладная,Расходно-приходная накладная,Накладная"` — mixed-case `"Торг-12"`, not `"ТОРГ-12"`, and no `"Счет-фактура"`/`"Чек"` values at all. Corrected `_normalize_document_form(...)` to match exactly, and found + fixed the identical wrong-case/wrong-value bug independently duplicated in two older heuristic functions that predate it: `_extract_document_form(...)` in `ocr_service.py` and `_detect_document_form_from_text(...)` in `invoice_review_service.py`. All other checked dropdowns (`Статус загрузки`, `Статус строки`, `Корректировка`, `Дубль`, `Товар найден в справочнике`) already matched backend constants exactly. Deployed; 90 targeted tests pass.
- Evidence-provider order was reverted from "MinerU first" back to "Google Drive OCR first, MinerU as fallback" per explicit user decision (the earlier order was a temporary trial). Rewrote the three tests in `test_document_extraction_service.py` that encoded the old attempt order; 91 targeted tests pass after the swap. Deployed.
- New VPS deployment path for handing the bot to a business analyst without depending on the developer's own machine or a purchased domain: `docker-compose.yml` gained a `caddy` service under a new `public-ip` profile, paired with a root `Caddyfile` and a `PUBLIC_DOMAIN` env var meant to hold a free nip.io hostname (e.g. `203-0-113-5.nip.io`) that resolves straight to the VPS's own public IP — Caddy then obtains a real Let's Encrypt certificate for it with zero DNS setup. Also added `CADDY_HTTPS_HOST_PORT` (for when 443 is already taken by another service on the box) and `BACKEND_MEM_LIMIT` (to cap the backend container on small/shared VPS boxes). Full step-by-step instructions are in `docs/wiki/runbook.md` under "VPS deploy for BA/tester access".
- **Live and verified**: the bot backend is now actually deployed at `https://78-17-160-248.nip.io:8443` on the user's own VPS (granted SSH access this session). That VPS turned out to be an active personal VPN server (`amnezia-*` containers, hostname `VpnServer`) with only 1.9 GB RAM and port 443 already taken — deployed anyway per explicit user confirmation, using `CADDY_HTTPS_HOST_PORT=8443`, `BACKEND_MEM_LIMIT=700m`, and no MinerU (Google Drive OCR only). Confirmed externally: `/health/runtime` returns `200` with a real trusted cert, `/bot/uploads/latest` correctly returns `401` without the shared secret and `404` (no history) with it, and `docker stats` shows no resource contention with the existing VPN containers. Full trace of what was built/blocked/fixed to get here is in `docs/wiki/log.md`, entry "deploy | live on user's VPS". Remaining: point n8n's `Workflow Config -> backendBaseUrl` at that URL and run a real end-to-end Telegram test with the business analyst — not done from this session.
- Latest commit on `over_version` is `d063b31` (2026-07-12, colleague Andrey Gomzikov, not yet reviewed live): "Add new product handling and package quantity extraction". Adds a local SQLite-backed reference-catalog cache (`ReferenceCatalogEntry` model + `reference_catalog_service.py`) that seeds/upserts product and supplier entries from the Google Sheet and does local fuzzy matching, wires it into `auto_fill_iiko_fields(..., db=db)` in `invoice_review_service.py`, and adds a new `Кол-во в упаковке` (units-per-package) column to both the register and shared-sheet row builders.
- **Regression found and fixed on 2026-07-14**: `_detect_document_form_from_text(...)` in `backend/app/services/invoice_review_service.py` had been reverted back to returning `"ТОРГ-12"` (uppercase) plus re-added `"Счет-фактура"` as a possible value by commit `d063b31`, undoing the 2026-07-09 fix that aligned this exact function to the live sheet's `Форма документа` dropdown (`"Торг-12"` mixed-case, no `"Счет-фактура"`/`"Чек"` values). Root cause: `d063b31` was a normal linear commit on top of the fix (not a merge), so the fix was present and then hand-reverted — most likely an unrelated side-effect of whatever tooling/edit Andrey used while building the package-quantity feature, since the touched lines have nothing to do with that feature. Re-applied the original `e1e07e2` fix verbatim (comment plus `"Торг-12"`, no `"Счет-фактура"` branch); `ocr_service.py`'s sibling function was already correct and untouched. Committed as `55c1023` and pushed to `upstream/over_version`.
- Unrelated pre-existing test failure noticed while verifying the fix: `backend/tests/test_receiving.py::test_invoice_review_sheet_clears_non_visible_values_on_torg12_continuation_page` fails with `KeyError: 'Грузополучатель'` both with and without this fix applied (confirmed by testing the exact `d063b31` version of the file) — so it's a separate, already-existing gap (missing/renamed column in the `Накладные` sheet header from `d063b31`'s own changes) worth a dedicated look, not something this fix caused or should fix.
- Working tree on `over_version` (as of 2026-07-14) has uncommitted local state: `autosnab_mvp.db` modified, root xlsx copies `Копия АвтоСнаб Кафе Ромашка .xlsx` / `Копия АвтоСнаб Кафе Ромашка  (2).xlsx` staged as deleted, `Копия АвтоСнаб Кафе Ромашка 3.xlsx` modified, and a new untracked `АвтоСнаб Кафе Ромашка  (ориг).xlsx` at repo root (same display name as already-registered `src_bd91ee3517`, but at root instead of `inbox/` — needs a fresh registration check if it's actually a new/different file before compiling anything from it).
- **2026-07-14 VPS remediation session** (same day as the two entries above, on the deployed `78.17.160.248` box): a real bot upload (`УТ-35634`, `ООО "Птицеводческий комплекс 'Продукты питания'"`, sum `4117.06`) failed to reach the Google Sheet. Root-caused and fixed three independent, stacked problems in order:
  1. **Google OAuth `redirect_uri_mismatch`**: the VPS's OAuth client in Google Cloud Console never had `https://78-17-160-248.nip.io:8443/api/v1/google-oauth/callback` registered (only the developer's local redirect URI was). User created a fresh OAuth client (`78170315728-ujd06n6ffnvo4t0qai4lu84ciq6nsie4.apps.googleusercontent.com`, project `autosnab`) with the correct redirect URI baked in from the start; server `.env` `GOOGLE_OAUTH_CLIENT_ID`/`CLIENT_SECRET`/`AUTH_URI`/`TOKEN_URI` updated and old (dead) access/refresh tokens cleared to force a clean re-auth.
  2. **`oauthlib.oauth2.rfc6749.errors.InsecureTransportError: OAuth 2 MUST utilize https`** on the callback even with the new client: uvicorn behind Caddy had no proxy-header trust, so `request.url` inside FastAPI resolved to `http://` (Caddy terminates TLS, backend only ever sees plain HTTP inside the docker network). Fixed by adding `--proxy-headers --forwarded-allow-ips=*` to the uvicorn CMD in `Dockerfile`. (`forwarded-allow-ips=*` is a deliberate small-project trade-off — port 8000 is also host-exposed directly, so this trusts `X-Forwarded-*` from any source; acceptable here, would need scoping to the docker bridge subnet for a higher-value target.) OAuth now survives a full container recreate (verified twice).
  3. **Live spreadsheet header drift**: after OAuth was fixed, the write still failed with `В листе 'Накладная' отсутствуют обязательные заголовки: Товар найден в справочнике, Кол-во в упаковке`. Direct Google Sheets API read of the live `Накладная` row 2 showed the actual production sheet has **43 columns**, not the 41 hardcoded in `SHARED_INVOICE_HEADERS` — someone had manually renamed `"Товар найден в справочнике"` → `"Статус сопоставления товара"`, renamed+repositioned `"Кол-во в упаковке"` → `"Состав упаковки"` (moved earlier, before `"Кол-во в документе"`), and added two brand-new columns `"Код товара УС"` (row-1 comment: "загружаем из УС" — pulled from the matched catalog entry) and `"ID строки"` (row-1 comment matches `"ID документа"`'s wording: service-assigned, sequential-or-random). A root xlsx the user attached (`АвтоСнаб Кафе Ромашка  (1).xlsx`, registered as `src_20260714_workbook1`) turned out to be *older* than the live sheet (still 40 columns, no `Кол-во в упаковке` at all) and was **not** used as the source of truth — the live Google Sheets API read was used instead, since that's what the backend actually writes against.
- **Root cause of the header drift**: not identified with certainty — most likely a manual edit to the live spreadsheet (by Andrey or the business side) that was never synced back into `SHARED_INVOICE_HEADERS`, made independently of code changes. Confirmed the Apps Script (`checkSelectedDocuments`/`loadSelectedDocumentsToTestSheet`/etc., pasted by the user) reads columns **by name** (`getColumnMap_`) and does not require or reference any of the four changed/new columns in its `requireBaseColumns_`/`exportHeaders` lists, so the Apps Script itself is unaffected by the rename either way.
- **Fix applied and verified end to end**: `SHARED_INVOICE_HEADERS` in `google_sheets_service.py` rewritten to the exact live 43-column name/order (confirmed via direct API diff: 0 missing, exact order match). `_shared_invoice_item_row` and `_remap_source_rows_to_shared_sheet` (`invoice_review_service.py`/`google_sheets_service.py`) updated: renamed dict keys, added `"Код товара УС"` (sourced from the already-existing `row_meta["product_code"]`, itself populated by `item_normalization_service.apply_reference_mapping_to_payload` from the matched `Товары` catalog row's `Код` column — this field existed end-to-end already, just wasn't wired into the sheet row) and `"ID строки"` (sourced from `ReceivingItem.id`, the DB primary key — mirrors how `"ID документа"` already uses `receiving.id`). The single-column self-heal helper (`_insert_units_per_package_column`, triggered when exactly one expected header is missing) was repointed from the old name to `"Состав упаковки"`. `INVOICE_REGISTER_COLUMN_WIDTHS` was refactored from hardcoded-by-position to a name-keyed dict resolved via `SHARED_INVOICE_HEADERS.index(...)` at import time, specifically so a future header reorder/insertion cannot silently misalign formatting again. The **legacy** `INVOICE_REGISTER_HEADERS` list (targets the old `"Накладные"` register sheet, confirmed not the live write path — its own hardcoded-index helpers `_next_document_id`/`_with_document_id` were left untouched) was deliberately left with the old names, since it's a separate, inactive contract.
- Verified live: after redeploy, a direct Google Sheets API read confirmed `SHARED_INVOICE_HEADERS` now matches the live row 2 exactly (43/43, 0 missing, order-identical). Re-triggered `POST /invoice-review/10/google-sheet` (no need to re-upload through the bot) and confirmed the `УТ-35634` document wrote correctly into row 3, with every field landing in its correct column including the two new ones (`ID строки: 44`, `Код товара УС` empty because that item wasn't matched in the product catalog).
- Test suite: `test_google_sheets_service.py` had four hardcoded old-column-count assertions (`41`, `"AO"`, `40`, `[""] * 41`) that needed updating to derive from `len(SHARED_INVOICE_HEADERS)` instead of hardcoding — now self-adjusting. Confirmed zero new failures introduced in `test_receiving.py` (same 17 pre-existing failures with and without this change, identical test names, verified via `git stash`).
- All of today's fixes are deployed and confirmed live on `78.17.160.248`: OAuth client swap, proxy-headers fix, and the 43-column header contract sync are all running in the current `autosnab_backend_mvp4` container.
- **2026-07-17 field feedback from Lilia** (real tester, ~15 repeated uploads of one ИП Минибаев invoice): five distinct symptoms reported, compiled into `docs/wiki/lilia-feedback-2026-07-17-parsing-instability.md`. Two are new and untracked (quantity digit drift like `5,000` → `5,001` across line items on repeat uploads; item row order sometimes scrambled, top row landing last). One is partially tracked (wrong document/line totals — overlaps `invoice-recognition-hardening-plan.md` Phase 5, not yet fully verified). One (persistent `Дубль` after deleting prior uploads) is confirmed expected behavior by Lilia, not a bug.
- **2026-07-17 investigation of the two-page (multi-page) issue**: ruled out a draft-append race condition (single uvicorn worker, no `await` inside `append_bot_draft_page`'s handler body means concurrent requests can't interleave mid check-then-create) and ruled out "no images sent to OpenAI" (see corrected note above — images already go out). **Found and fixed a real gap**: `normalize_invoice_result` already flagged empty `document_date`/invalid `supplier_inn`, but never flagged empty `supplier_name` or `document_number` — exactly two of the four fields Lilia reported as missing on failed multi-page uploads. A document with those fields blank could previously be created and written to the sheet without ever being marked `Требует проверки` for that reason. Fixed in `backend/app/services/invoice_normalization_service.py` (two new `_flag(..., "error")` calls) with two new regression tests in `test_openai_invoice_pipeline.py`; 125/125 non-`test_receiving.py` tests pass, `test_receiving.py`'s 8 pre-existing failures unchanged (verified via `git stash`). **Not yet explained**: why OpenAI's header extraction is actually unreliable specifically on the merged 2-page request (~14/15 failure rate) — needs a live repro with the real invoice file and an `exports/openai_debug` trace of a failing run to pin down further; not done this session (no live VPS/file access in this session).
- **2026-07-17 repro of `openai` mode (single-page, 8 runs)**: ran the already-registered `УПМК003248` page-1 fixture (`5321471953447098411 (1).jpg`, `src_c408edb166`) through `extract_invoice_document(..., extraction_method="openai")` 8 times locally. Supplier/INN/document number/date/total_sum and all item quantities/prices/amounts were byte-identical across all 8 runs — the digit-drift Lilia reported did not reproduce on this file. Only `raw_name` text varied: 3/8 runs prefixed the item name with its row number, and 4/8 runs dropped the `с/к` abbreviation's slash (`"с/к"` → `"ск"`) — cosmetic OCR/model text variance, not numeric drift.
- **2026-07-17 repro of `hybrid` mode (same file, 8 runs)**: same fixture through `extraction_method="hybrid"` — see `invoice-recognition-hardening-plan.md` Blocker 7. Result was deterministically empty on all 8 runs (no supplier, no INN, no items, fake document number/total). Root cause: MinerU's HTML-table output isn't compatible with the legacy regex parser used downstream. `hybrid`/`mineru` is not currently usable as a real fallback for this document type; `openai` remains the only working mode for scanned rotated forms like this one.
- **2026-07-20 SBIS EDO planning session**: prepared a teamlead status report (`docs/reports/status-report-2026-07-20.txt` + short version) covering everything currently implemented and working, then separately scoped SBIS EDO integration. Confirmed `upstream/main` is stale (Andrey's last commit there is `06454ca`, 2026-07-04) — his real latest work (`d063b31`, 2026-07-12) is on `upstream/over_version`, which is also ahead of `main`. Confirmed via official Saby docs that a dedicated EDO API test stand exists (`fix-online.sbis.ru`, same method contract as production `online.sbis.ru`), though how to obtain test credentials for it isn't publicly documented — requires contacting Saby support. Recorded a full effort estimate (~1.5-2.5 developer-weeks to a one-document MVP, main risk is SBIS auth/access discovery, not in-repo code) in `docs/wiki/sbis-edo-integration.md` under "Effort estimate (2026-07-20)". No code changes.
- **2026-07-20 later same day — `upstream/main` moved again**: Andrey pushed a full Diadoc (Kontur) EDO integration to `main` (commits `003b736`/`a2f1ebb`/`8b9b4c1`/`0d0fd63`). `main`'s git history was reset first (`003b736 Clean main branch` deletes almost everything), then re-added as one large snapshot commit — `main` is no longer fast-forwardable from earlier `main` and should be treated as a fresh snapshot to diff against, not merged blindly. Full technical writeup, including a concrete unresolved risk, is in the new `docs/wiki/diadoc-integration.md`.
- Diadoc summary: OIDC auth, scheduler polling `V8/GetNewEvents` every 300s, `V4/GetEntityContent` downloads, formalized-XML-first parsing with PDF/image fallback through the existing extraction pipeline, order matching by basis-document number, decoupled Google Sheets/print-form delivery with retry + `dead_letter`, admin endpoints gated by `X-Diadoc-Api-Key`. Andrey's own wiki claims 187 passing backend tests as of 2026-07-20 but no live-mailbox smoke test yet (no live credentials available to him either).
- **Concrete gap found in this review, not yet fixed anywhere**: `upstream/main`'s `SHARED_INVOICE_HEADERS` in `google_sheets_service.py` is still the pre-2026-07-14 41-column contract (missing `"Статус сопоставления товара"`, `"Код товара УС"`, `"Состав упаковки"`, `"ID строки"` renames/additions that `over_version` applied after the real spreadsheet was found to have drifted), and `_detect_document_form_from_text` still has the `"ТОРГ-12"`/`"Счет-фактура"` regression that `over_version` fixed twice. If Diadoc's Sheets delivery is pointed at the real production sheet in its current `main` form, it will fail with the same `отсутствуют обязательные заголовки` error hit on 2026-07-14. This needs reconciling — most likely cherry-picking Diadoc-specific files onto current `over_version` rather than merging `main` wholesale — before Diadoc goes live.
- This also raises an open business question worth confirming with the user/BA: Diadoc (Kontur) and SBIS (Saby/Tensor) are two different EDO providers. Since Diadoc is now built, it's worth checking whether SBIS is still a required parallel integration before spending the ~1.5-2.5 dev-week estimate on it.
- **Correction (2026-07-20, later session)**: the wiki commits that followed the Diadoc transplant (`c33de9a`, `896f263`) claimed the transplant was "done" but never actually merged `diadoc-integration` into `over_version` — verified via `git cat-file -e HEAD:backend/app/services/diadoc_client.py` (missing). Fixed: `git merge diadoc-integration` is now genuinely on `over_version` (clean merge, no conflicts). Re-verified: 29/29 (`test_diadoc_*` + `test_google_sheets_service.py`), full suite 181 passed / 8 failed (same pre-existing `test_receiving.py` failures as before the merge). See [[diadoc-integration]] for the corrected status.
- **Done**: `../autosnab-core` is now a populated, local git repo (`main` + `develop`, one clean initial commit `9a5e97a`), reorganized by functional domain (`invoice_pipeline`, `google_workspace`, `edo_diadoc`, `iiko`, `accounting_backoffice`) with no wiki system carried over — see its `docs/ARCHITECTURE.md`. Verified equivalent to `over_version` at migration time: identical pytest pass/fail set (181/8). `autosnab_mvp` remains the working/session repo; `autosnab-core` has no GitHub remote yet (local-only per user decision). Any future code change made to `over_version` after 2026-07-20 is **not** automatically reflected in `autosnab-core` — it's a point-in-time snapshot, not a synced mirror.
